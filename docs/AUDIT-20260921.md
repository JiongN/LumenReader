# LumenReader 全面审计与修改记录

## 范围与结论

本次审计覆盖 SwiftUI 窗口与标签状态、PDFKit / WebKit 阅读链路、EPUB 解包与内链、AI 会话和翻译、持久化、更新与发布脚本、主要 UI 入口及工程目录。当前版本已修复会导致 EPUB 翻译失效、解包越界、持久化静默丢数据和发布误报成功的高优先级问题。测试、Debug / Release 构建、签名校验与 EPUB 逐段翻译端到端检查均通过。审计计划尚未全部闭环：烟测汇总仍主要依赖日志中的 `❌` / fatal 标记，个别只输出结构化 `pass=false` 的诊断存在假通过边界；本轮未重复执行该旧审计流程。

审计不等于穷尽所有第三方 PDF / EPUB 变体、所有 macOS 小版本和真实网络服务。下文会明确区分已修复项与剩余风险。

## 主要发现与已实施修复

| 级别 | 问题与影响 | 修改方式 | 状态 |
| --- | --- | --- | --- |
| P0 | EPUB ZIP 条目可能通过路径穿越、符号链接、异常压缩比或过大解包体积影响文件系统与稳定性。 | 在解包前预检中央目录，限制条目数、单文件和总体积，拒绝 ZIP64、分卷、加密、链接和越界路径；串行化解包。 | 已修复，有边界回归测试 |
| P0 | EPUB 设置默认为 Apple 系统翻译，原控制器却忽略引擎选择并始终调用 Microsoft Web 通道；页面未就绪和网络失败又被统一吞成“翻译失败”。 | EPUB 翻译改为真实遵循 Apple / Microsoft / LLM 选择，支持缓存、长文分块、4 路网络并发、取消世代保护、具体错误和重试。WebKit 未就绪时显式报错。 | 已修复，端到端验证 3/3 段成功 |
| P1 | EPUB 段落选择包含嵌套 `li` 与子段落，可导致重复翻译；部分商业 EPUB 用 `div` 直接装摘要正文，又会出现“标题译了、正文没译”。 | 段落识别扩展到叶级 `div/section/article`，并跳过已包含更精确块的父容器；只为实际待译索引插入占位。 | 已修复 |
| P1 | EPUB 翻译位于“格式”弹窗，与 PDF 翻译交互不一致，也把内容操作混入排版设置。 | 从格式弹窗移除，EPUB 与 PDF 都通过左侧工具栏翻译标签打开；面板提供开关、语言、引擎、进度、停止和重试。 | 已修复 |
| P1 | 损坏的持久化 JSON 可能被当成空数据后覆盖，无法诊断或恢复。 | 严格解码核心集合；无法解码时保留 `.corrupt-*` 备份，再显式回落。 | 已修复 |
| P1 | 某些日志将外部文字直接当格式字符串，百分号可导致异常输出甚至崩溃。 | 所有动态日志改用常量格式 `"%@"`。 | 已修复 |
| P1 | 发布脚本可能在上传条件不足时留下 tag，在 HTTP 失败时继续报成功，或因变量后紧跟中文标点而被 Bash 误解析；产物分散。 | 严格校验版本、工作区、tag、签名与 HTTP 状态，对插值变量使用花括号边界；本地审阅模式不联网；ZIP、DMG、校验和构建信息集中到 `releases/<version>/`。 | 已修复 |
| P2 | 文档、过期方案、当前验证和发布产物职责混杂。 | 当前文档保留在 `docs/` 顶层，历史记录转入 `docs/archive/`，本地验证输出转入忽略的 `docs/verification/`，分发产物集中到 `releases/`。 | 已整理 |

## 架构与 UI 评估

分层总体合理：`LumenKit` 承载可测试的核心能力，`LumenApp` 承载平台 UI 和控制器，每个文档由 `ReaderSession` / `ReaderBridge` 统一 PDF 与 EPUB 能力。最大的长期耦合点位于较大的 SwiftUI 视图、应用内诊断程序与主目标同编译，以及全局 AI 会话和窗口级文档状态之间的边界。它们目前不构成已知崩溃点，但会增加后续改动的回归风险。

UI 的主导航与 macOS 桌面交互一致。本轮把 EPUB 翻译收敛到和 PDF 相同的左侧工具栏，避免功能重复和概念冲突。新应用图标以 `branding/lumen-icon-source.png` 为唯一源稿，`branding/render.js` 生成标准 iconset 和 `Resources/AppIcon.icns`，构建时再复制到 app bundle。

## 剩余风险与建议改法

1. **验证结果假通过（P1）**：`tools/run_smoke.py` 目前根据退出码、完成标记和日志中的 `❌` / fatal 文本判定；只输出 `pass=false` 而不带失败标记的诊断可能被算作通过。建议让每个诊断统一返回机器可读结果和非零退出码，再由烟测严格解析。
2. **大型 PDF 批注保存卡顿（P1）**：PDFKit 对原文件的整体写回仍可能占用主线程。建议增加带文档世代号的后台快照写入，写入前保留同目录临时副本，并用 500 MB 以上的含批注文件做负载测试。
3. **EPUB 书内脚本（P1）**：WKWebView 仍允许 EPUB 正文脚本执行。现有导航规则阻止未经用户点击的外部页面替换，但不能消除恶意书籍的所有行为。建议默认移除 `script`、内联事件和可执行 URL，只向明确白名单文档开放交互。
4. **EPUB 格式覆盖（P2）**：为安全和可预测性，当前拒绝 ZIP64、分卷、加密、链接和解包总量超过 512 MB 的书。这是有意限制。若需扩大覆盖，应优先接入受维护的 ZIP 库并保留当前额度，不要放宽外部命令路径。
5. **会话共享语义（P2）**：AI 会话是进程级共享的，可混合多本文档。已有引用来源校验，但用户仍可能对当前上下文产生误判。建议在会话标题附近持续显示当前文档范围，并为新文档提供一键新会话。
6. **Swift 6 并发迁移（P2）**：两个产品目标仍以 Swift 5 语言模式构建。建议分模块开启严格并发警告，先处理存储和翻译任务的 `Sendable` / actor 边界，再切换整个工程。

## 验证记录

- `swift test --disable-sandbox`：62 个测试通过。
- `./build.sh release`：应用组装完成；最终发布包另行复核 `codesign --verify --strict --deep dist/Lumen.app`。
- EPUB 翻译端到端：用模拟 OpenAI 兼容服务打开同时含 `p` 与叶级 `div` 正文的测试 EPUB，左侧翻译面板显示 `3 / 3` 段，页面译文块 3、失败 0、加载中 0。
- PDF 性能：Release 构建下以 120 页文档驱动 60 步连续滚动，主线程停顿 p95 `5.34 ms`，超过 16.7 ms 的样本 `3 / 92`。这些是定时器延迟指标，不是实际显示帧率，不能证明快速滚动流畅。2026-09-22 用户实测仍有严重卡顿，本项尚未验收。随后用真实长篇 PDF、2 倍缩放、120 步滚动复测，关闭侧栏和 AI 面板后，主题/原色两组 CPU 分别为 `29.32 / 29.40 ms/步`，主线程停顿 p95 为 `1.16 / 0.80 ms`，说明主线程延迟低仍可伴随高后台渲染开销；具体瓶颈仍需用户复现与采样确认。已有缩略图暂停措施只解决一部分竞争，不能据此宣称问题已解决。
- 图标管线：新 PNG 源稿生成全部 16–1024 pt iconset 和 `Resources/AppIcon.icns`，已随构建复制入 app bundle。
- 本地发布包的 ZIP、SHA-256、构建信息与签名复核保存在 `releases/<version>/`；本次未推送 tag，未创建 GitHub Release。
