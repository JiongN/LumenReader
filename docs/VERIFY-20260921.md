# 2026-09-21 修复与验证

交付：`dist/Lumen.app`，Release 构建标记 `20260921-095306-10897`。保留原版图标及用户/其他 AI 的并行改动；未重复测试已确认正常的面板拖动。

## 修复

- 用户实机对照已确认：原色 PDF 流畅，主题色导致卡顿。移除滚动树的 Core Image 滤镜及后来尝试的整屏 SwiftUI 混合，改用公开 PDFDocumentDelegate / PDFPage 绘制钩子，在 PDFKit 原生 tile 生成时处理色调，随后复用 tile。缩略图独立文档后台渲染、缓存着色结果；隐藏时停止无消费者的视口计算，过期请求在渲染前取消。
- 保存 PDF 时采用线程局部原色作用域，OCR 显式原色绘制。主题不写入原文件。切换主题恢复页内位置、手动倍率与文字选择。
- 附件崩溃栈指向 PageJumpPanel 的 EnvironmentObject.error。改为显式状态依赖，并将根环境注入移到覆盖层之后；同时避免输入 Int.min 时减一溢出。
- 同一页跨行高亮生成一个带多个 QuadPoints 的标准批注；跨页仍按页存储。新批注使用可持久化的 /LumenID；实测 PDFKit 会丢弃 /NM，因此没有使用它做稳定身份。历史 Lumen 连续碎片保守分组，编辑/删除作用于整组，不在打开文档时重写原件。
- 批注跳转使用原生矩形导航并预留可见边距；正文命中高亮时定位左栏条目，重复点击也能重新滚到选中项。异步列表加载取消后不再发布旧结果。
- AI 输入区模型选择、输入框、发送按钮统一最小高度与中心对齐。清除过时钥匙串提示和无引用实现，修复控制器回调引用及关闭后任务/文档释放路径。保留其他 AI 新增的全局会话设计。

## 检查结果

| 检查 | 结果与证据 |
|---|---|
| 单元测试 | 29 项通过，`verification/20260921/tests.log` |
| 批注原有回归 | 36 项通过，`annotations-final.log` |
| 跨行分组、保存身份、联动、跳转 | 18 项通过，同上 |
| PDF 搜索 | 6 项通过，同上 |
| 五主题、保存像素/文字、OCR、阅读位置、选择、资源释放 | 40 项通过，`regression.log` 的 tone 部分 |
| 快捷键与会话生命周期 | 55 项规则通过；迁移保留、关闭取消、幂等检查通过，同上 |
| Release | 构建成功，签名严格校验通过，`release.log` |
| 修改检查 | `git diff --check` 通过；Resources/branding 无差异 |

`regression.log` 中早期批注失败已由后续修复和 `annotations-final.log` 覆盖；保留原日志以便追溯。

实际窗口验证：Cmd+G 普通跳页、极小负数页码均正常；测试文档跨行高亮仅一条左栏记录，点击后从第 1 页到第 2 页，目标完整进入视口；正文高亮点击后左栏有选中描边。真实屏幕确认暖纸与深色着色。应用自身 cacheDisplay 截图绕过 PDFKit 已着色 tile，不能用于判断主题显示。验证仅写临时测试 PDF，未修改用户原书。

## Release 滚动对照

同一本约 20 MB《教育的目的》扫描 PDF，1400×900 窗口，160 次原生滚动事件，每步 420 像素，顺序独立启动，均正常退出。没有执行面板拖动。日志为 `verification/20260921/tile-*.log`。

| 模式 | 主线程延迟 p95 / 最大 ms | 全线程 CPU ms/步 | 最大 RSS MiB |
|---|---:|---:|---:|
| 原色、面板关闭 | 0.94 / 278.78 | 10.60 | 254.5 |
| 暖纸、面板关闭 | 0.95 / 35.12 | 10.36 | 234.1 |
| 暮蓝、面板关闭 | 0.94 / 10.01 | 10.11 | 253.2 |
| 暖纸、双面板开启 | 1.49 / 30.83 | 13.22 | 329.1 |

主题与原色的滚动 CPU 开销接近。RSS 包含启动、滚动、截图的整个进程峰值，不能当作稳态内存或断言固定降幅。双面板改变阅读宽度和倍率，不能与全宽模式直接计算性能退化百分比。原色单次最大延迟较高也说明冷启动/调度噪声，不能挑它宣称主题更快。

这是一轮可重复的自动事件对照，主线程计时不是显示帧率，驱动强度低于此前真实触控板采样；不据此宣称已绝对无卡顿。最终触控板手感仍应由用户在新版主题模式下确认。资源释放检查通过，但有限回归不能保证所有文档、长时间运行均无崩溃。

## 已知边界

- 旧文件若没有身份信息，只能依据作者、时间、颜色和相邻几何保守合并；来源不明或不满足条件的旧碎片仍独立显示，避免误合并其他批注。
- 整页色调会改变彩图颜色；原色开关仍保留。跨页选区按 PDF 页分别保存。
- 本轮未对真实 API 发收费请求；保留既有 EPUB/AI 功能，未把整个仓库的所有并行修改都声称为本轮新增或完整人工验收。

## 视觉与 AI 增强（10:44 增补）

- 全局 UI 令牌字号与 SF Symbols 统一增大 1pt；顶部单行栏增高到 48pt，左侧图标栏基准宽度
  56pt。普通 1400×900 截图中，标签、工具栏、左栏和 AI 控件无截断。
- 窗口宽度超过 1320pt 后，图标栏、左栏及 AI 栏按同一系数放大，最高 1.22 倍。1900pt
  实测：图标栏 68.3pt、左栏 302.6pt、AI 栏 463.6pt、正文 1065.5pt；940pt 最窄窗口
  仍保留 320pt 正文。`--resize-report 1` 共 12 项通过、0 失败。
- AI 动作改为“总结当前页/章”和“总结全文”；提问可明确选择“全文检索”或“当前页/章”。
  输入立即限制在 6000 字，超过时提示把长材料作为文档打开，避免超长粘贴拖死界面。
- 全文总结不再受旧切片方式造成的“像只读 20 页”问题影响。`SummarySlicePlanner` 将任意页数
  均衡分成最多 30 组，每页/章恰好覆盖一次；1、20、31、120、600、2001 个单元均已做
  覆盖测试。长文每单元提供代表性文字后 map-reduce，仍受服务商上下文、输出长度与费用限制。
- 一个服务商可以保存、获取、删除并切换多个模型。Agent 增加逐条自定义技能，并补充“理论
  精读助手”“研究写作编辑”预设；旧配置缺少新字段时容错解码。
- Impeccable 静态视觉检测返回 0 项；原版图标资源无差异。完整干净重编译另发现并修复
  `PDFParagraphExtractor` 把文本行对象误传给 `CGRect.union` 的编译错误。

验证结果：33 项单元测试通过；入口审计 12/12；响应式布局审计 12/12；
`git diff --check` 通过。界面截图：`/tmp/lumen-ui-normal.png`、`/tmp/lumen-ui-wide.png`。

## PDF 对照翻译与 AI 面板最终增补（16:50）

### 功能结果

- PDF 新增当前页段落对照栏。文字层可信时直接抽取；不可信时使用 Vision OCR。译文仅保存
  在应用数据目录的文档缓存中，不修改 PDF，不覆盖页面，也不接管 PDFKit 的选择、链接、
  搜索与批注。
- 默认引擎为 Apple 系统翻译，系统语言包下载后可离线；微软在线实现作为后备。Apple 会话
  由 `translationTask` 持有，视图消失即取消，避免在失效 session 上继续调用。
- AI 面板改为紧凑两行头部：联网、模型、会话在第一行，模板与 Agent 在第二行；底部范围
  按钮位于输入框左侧且等高。提问默认当前页/章，可切全文检索或双文档对照。
- 自定义问题在没有选区时也携带所选文档上下文；双文档桩服务请求已确认同时包含文档 A、
  文档 B 与“对照”标识。总结全文：短文直接传入，长文才使用最多 30 个均衡 map 分段。

### 本轮证据

| 检查 | 结果 |
| --- | --- |
| Swift 单元测试 | **43/43** 通过，含 Apple 默认/微软后备、目标语言缓存与自定义问题上下文 |
| PDF 段落审计 | **42/42** 通过；真实夹具 2 页、25 行 → 9 段，字符覆盖率 100.1% |
| 翻译端到端 | 英文 PDF **2/2 段**译为中文并缓存；截图 `/tmp/lumen-translation-final.png` |
| AI 常规窗口 | 1180×760 无溢出、无遮挡，截图 `/tmp/lumen-ai-final.png` |
| AI 最小窗口 | 940×640 无溢出、输入区完整，截图 `/tmp/lumen-ai-min-final.png` |
| 入口归属 | **12/12** 通过，三个点按钮已删除且导出/会话入口无重复 |
| 响应式宽度 | **12/12** 通过；940pt 保留 320pt 正文，拉宽后按比例恢复 AI 面板偏好 |
| 批注回归 | 基础 **36/36** + 跨行分组/定位 **18/18**；定位目标进入可见视口且保持手动缩放 |
| 会话关闭 | 迁移保活、关闭取消、重复关闭三项均通过 |
| 暖纸 PDF 滚动 | 《教育的目的》120 步：p95 0.99ms，>16.7ms 1/162；阅读区、AI、侧栏、缩略图与 PDF representable 均为 0 次/步重算 |

自动滚动驱动强度约为真触控板的 0.45 倍，不能替代手感结论；它用于证明主题渲染不再触发
SwiftUI/PDFView 的逐步布局。Apple 首次翻译某语言对会出现系统下载提示。双栏、表格、复杂
页眉页脚或 OCR 误识别仍可能影响段落边界，应以对照栏中的原文为准。

最终 Release 构建标记为 `20260921-165539-50494`，`codesign --verify --deep --strict`
通过，签名为 `Lumen Dev`。发布包分别以 1180×760 的 PDF 翻译和 940×640 的 AI 面板启动，
均正常打开、截图并自行退出；证据为 `/tmp/lumen-release-translation.png` 与
`/tmp/lumen-release-ai.png`。`Resources/branding` 无差异，继续使用原版应用图标。
