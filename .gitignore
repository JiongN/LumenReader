# ── 构建产物 ──────────────────────────────────────────────
# SwiftPM 的中间目录，实测 362M，且完全能由 `swift build` 重建。
# 这是本仓库体积的大头，不排除掉的话每次 clone 都要白拉三百多兆。
.build/
.swiftpm/

# build.sh 组装出来的 .app。里面是 ad-hoc 签名，
# 换台机器或换个系统版本都得重新签，入库没有意义。
dist/

# tools/make_icons.swift 的产物目录：图标 PNG 与 icon.iconset。
# 注意和 .build/ 区分——这个 build/ 不是 SwiftPM 的中间目录，
# 而是我们自己脚本写出来的中间 PNG；能由 `swift tools/make_icons.swift`
# 完整重建，和 dist/ 同理不入库。
build/

# 注意：Package.resolved **不**忽略。
# 依赖锁定文件是构建可复现性的一部分，应当入库。

# ── 系统杂物 ──────────────────────────────────────────────
.DS_Store

# ── 编辑器 ────────────────────────────────────────────────
*.xcuserstate
xcuserdata/
.vscode/
.idea/

# ── 本地自检产物 ──────────────────────────────────────────
# 自检截图与日志默认都写在 /tmp，这里只是兜一道底——
# 免得哪天有人把 --capture 指进仓库，把几百兆 PNG 提交上来。
*.log

# 自检产物的存放目录。日志已被上面那条兜住，这里补的是截图 PNG——
# 一张动辄几百 KB～1MB，2026-09-19 那一轮 before/after 对比一共 12M。
# 证据留在磁盘上供人翻就够了，入库之后 12M 就永远在 .git 里出不来了。
docs/verification/
